# ⚔️ Pokemon Combat Simulator

This repository contains our submission for the Pokemon Combat Simulator group assignment. It features a complete implementation of all requirements, including API data fetching, stat visualization, and a robust combat engine.

## 🔗 Streamlit App URL
[Click here to view the deployed Streamlit Dashboard](https://your-streamlit-app-link-placeholder.streamlit.app)

## 📌 Group Contributions
Here is the summary of what we accomplished:

| Team Member | Contributions |
|-------------|---------------|
| **Member 1** | Built robust API Logic (`requests`, `@st.cache_data`, `try/except`) & Scaffolded strict Repo structure matching rubric. |
| **Member 2** | Engineered complete Pandas logic (`.melt()` transformations, precise `battle_log` dictionaries, and tidy HP format arrays). |
| **Member 3** | Designed Streamlit UI (`st.columns`, widgets, error handling) & integrated top-tier Plotly interactive graphs. |
| **Member 4** | Programmed full Combat Engine Mechanics (calculating damage, decoding type relations for double-typing, simulation loops). |

## 🚀 How to Run Locally

If you'd like to test the app locally, follow the steps below:

1. **Clone the repository:**
   ```bash
   git clone https://github.com/your-username/pokemon-combat-simulator.git
   cd pokemon-combat-simulator
   ```

2. **Install Python dependencies:**
   Make sure you have python installed. Then, install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

3. **Start the simulator:**
   ```bash
   streamlit run dashboard.py
   ```
